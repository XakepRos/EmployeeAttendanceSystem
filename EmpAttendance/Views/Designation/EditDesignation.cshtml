@model EmpAttendance.ViewModel.DesignationViewModel
@using (Html.BeginForm("EditDesignation", "Designation", FormMethod.Post, new { id = "EditDesignationForm" }))
{
    @Html.Partial("_Form", Model);
}

